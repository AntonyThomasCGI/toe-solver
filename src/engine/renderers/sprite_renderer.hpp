#pragma once

#include <glm/glm.hpp>

#include "geometry/mesh.hpp"


class SpriteRenderer
{
public:
    SpriteRenderer();
    ~SpriteRenderer();

    void render();

private:
    //Shader shader;
    //Mesh mesh;
};
